<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For Vien 💖</title>

<style>
    body {
        margin: 0;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: linear-gradient(135deg, #ff9a9e, #fad0c4);
        font-family: 'Georgia', serif;
    }

    .book {
        width: 350px;
        height: 450px;
        position: relative;
        perspective: 2000px;
        cursor: pointer;
    }

    .page {
        width: 100%;
        height: 100%;
        position: absolute;
        border-radius: 10px;
        transform-origin: left;
        transition: transform 1.2s ease;
        box-shadow: 0 10px 25px rgba(0,0,0,0.3);
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 25px;
        box-sizing: border-box;
    }

    /* COVER */
    .cover {
        background: #ff6f91;
        color: white;
        z-index: 3;
        flex-direction: column;
    }

    /* INSIDE PAGE */
    .inside {
        background: #fff8f8;
        color: #ff4f7a;
        z-index: 1;
        transform: rotateY(180deg);
    }

    /* MESSAGE (hidden at first) */
    .message {
        opacity: 0;
        transform: translateY(20px);
        transition: opacity 1s ease, transform 1s ease;
    }

    /* OPEN STATE */
    .book.open .cover {
        transform: rotateY(-180deg);
    }

    .book.open .inside {
        transform: rotateY(0deg);
    }

    .book.open .message {
        opacity: 1;
        transform: translateY(0);
        transition-delay: 0.8s; /* shows after page opens */
    }

    h1 {
        font-size: 2em;
    }

</style>
</head>

<body>

<div class="book" id="book" onclick="openBook()">
    
    <!-- COVER -->
    <div class="page cover">
        <h2>Click me 💌</h2>
    </div>

    <!-- INSIDE -->
    <div class="page inside">
        <div class="message">
            <h1> Happy Birthday Vien, my babyyy ❤️ 🎉</h1>
            <p>I just want you to know how much you mean to me. I’m so grateful that you gave me a chance, because having you in my life is something I’ll always be thankful for. Loving you feels so natural, and every day with you makes me appreciate you even more.</p>
        </div>
    </div>

</div>

<script>
function openBook() {
    document.getElementById("book").classList.add("open");
}
</script>

</body>
</html>